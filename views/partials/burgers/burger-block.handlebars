<li>
    {{name}}
    {{!-- The button below only gets created if the Devour is True --}}
    <button class="change-devour" data-id="{{id}}">
        {{#if devour}} 
            DEVOUR!
        {{/if}}
    </button>
</li>